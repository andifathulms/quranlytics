# Phase 4 semantic layer. On linux/arm64 the PyPI torch wheel is already
# CPU-only; on x86_64 it pulls CUDA extras but still runs on CPU.
torch==2.3.1
sentence-transformers==3.0.1
scikit-learn==1.5.0
